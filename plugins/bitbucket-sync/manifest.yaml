name: bitbucket-sync
version: 0.1.0
type: external-sync
description: Bitbucket issue synchronization

# Capabilities
capabilities:
  - sync.pull
  - sync.push
  - sync.delete
  - sync.bidirectional
  - auth.status

# Entry point
entry_point: ./bin/bitbucket-sync

# Dependencies
requires:
  tlc_version: ">=0.1.0"

# Configuration schema
config_schema:
  type: object
  properties:
    repo:
      type: string
      required: true
    sync_direction:
      type: string
      enum: [pull, push, bidirectional]
      default: bidirectional

# Permissions required
permissions:
  - network.http
  - credential.read
