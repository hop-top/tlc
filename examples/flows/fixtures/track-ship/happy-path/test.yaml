name: happy-path
description: |
  Track Ship flow — end-to-end track delivery happy path.

  Scaffold only: this fixture exists to satisfy
  TestAllFlowsHaveHappyPathFixture. Recorded LLM cassettes for the
  full procedure (create-worktree → implement → push → PR → merge →
  cleanup) have not been captured yet. Replaying this run will not
  succeed until the record/ directory is populated.
expected_exit: 0
