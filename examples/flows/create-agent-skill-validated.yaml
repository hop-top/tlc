flow_id: "flow:create-agent-skill-validated:1.0"
name: "Create Agent Skill (Validated)"
version: "1.0"
description: "Create a folder-based agent skill with eva gating and routellm routing"
entry_step: "decide-shape"

config:
  category: "documentation"
  triggers:
    - "create agent skill"
    - "add skill for this tool"
    - "create skill folder"
    - "document tool for agents"
    - "agentskills.io skill"

  validation_strategy: |
    ## Validation Strategy

    ### eva (deterministic gating)
    Applied after content phases to enforce agentskills.io spec compliance.
    Contracts live in evals/agent-skill/ and use only Tier 1 evaluators:

    - skill-frontmatter.yaml  — name format, description length, "Use when:" present
    - skill-structure.yaml    — required sections present, line length, word count
    - skill-scripts.yaml      — scripts are executable and print usage
    - skill-discovery.yaml    — description contains trigger keywords

    eva quick_validate.py covers the same ground for a fast local check;
    eva contracts are the authoritative gate for CI / pre-merge.

    ### routellm (LLM cost routing)
    Applied to the four generative steps. Content quality directly affects
    how useful the skill is to downstream agents.

    | Step | Threshold | Rationale |
    |------|-----------|-----------|
    | write-skill-md | 0.15 | Core skill body; errors cascade to every user session |
    | write-references | 0.50 | Deep-dive docs; weak model fine for structured reference |
    | write-scripts | 0.40 | Scripts need correctness; moderate quality bar |
    | write-description | 0.10 | Description is discovery-critical; always strong |

    research-errors and research-ops are deterministic/read-only — no routing.

  eva_contracts_dir: "evals/agent-skill"
  routellm_config: "config/routellm.yaml"

steps:
  # ── Phase 1: Decide shape ────────────────────────────────────────────────────

  decide-shape:
    step_id: "decide-shape"
    type: "branch"
    title: "Single file or folder skill?"
    cases:
      - when: "complexity == 'simple'"
        next: "simple-skill-path"
      - when: "complexity == 'complex'"
        next: "audit-tool"
    default_next: "audit-tool"
    meta:
      guidance: |
        Flat file when: ≤ 20 commands, no scripts, no reference docs,
        SKILL.md stays under 5k words. Folder otherwise.

  simple-skill-path:
    step_id: "simple-skill-path"
    type: "task"
    title: "Write single-file SKILL.md (simple tool)"
    task_ref: "task:simple-skill-path"
    meta:
      routellm_hint: "strong"
      routellm_model: "router-mf-0.15"
      guidance: |
        Route to strong model — even the simple path is discovery-critical.

        Write skill/<tool-name>/SKILL.md with frontmatter (name, description
        250-350 chars with "Use when:") and sections: When to Use, Quick Start,
        Core Commands table, Key Flags table, Common Workflows, Common Mistakes
        table, Error Handling table.

        Then jump to gate-structure; skip folder-path steps.

  # ── Phase 2: Audit tool surface ──────────────────────────────────────────────

  audit-tool:
    step_id: "audit-tool"
    type: "task"
    title: "Audit tool surface: CLI, API, scripts, error patterns"
    task_ref: "task:audit-tool"
    meta:
      tool: "shell --help sweep + just/make list"
      commands: |
        TOOL="<tool-name>"
        $TOOL --help > /tmp/${TOOL}-surface.txt
        $TOOL help --all >> /tmp/${TOOL}-surface.txt 2>&1 || true
        for sub in $($TOOL --help | awk '/^  [a-z]/{print $1}'); do
          echo "=== $TOOL $sub ===" >> /tmp/${TOOL}-surface.txt
          $TOOL $sub --help 2>&1 >> /tmp/${TOOL}-surface.txt
        done
        curl -s http://localhost:${PORT:-8080}/openapi.json \
          > /tmp/${TOOL}-openapi.json 2>/dev/null || true
        just --list > /tmp/${TOOL}-ops.txt 2>/dev/null || \
          make -qp 2>/dev/null | awk -F: '/^[a-zA-Z]/{print $1}' \
          >> /tmp/${TOOL}-ops.txt
      guidance: |
        - Read /tmp/${TOOL}-surface.txt to build command inventory (LLM step)
        - Trigger invalid ops to collect exact error messages

  # ── Phase 3: Research and scaffold (parallel) ────────────────────────────────

  research-parallel:
    step_id: "research-parallel"
    type: "parallel"
    title: "Research errors, ops, and scaffold folder"
    depends_on: ["audit-tool"]
    children: ["research-errors", "research-ops", "scaffold-folder"]
    max_concurrency: 3

  research-errors:
    step_id: "research-errors"
    type: "task"
    title: "Collect error messages and failure modes"
    task_ref: "task:research-errors"
    meta:
      guidance: |
        Deliberately trigger errors: missing flags, invalid values, wrong
        init order, auth failures, state machine violations.
        Record: exact error string, root cause, fix command.

  research-ops:
    step_id: "research-ops"
    type: "task"
    title: "Identify repetitive operations worth scripting"
    task_ref: "task:research-ops"
    meta:
      guidance: |
        Look for: every-project-setup ops, multi-step fixed-order sequences,
        typo-prone invocations, frequently-asked-about commands.
        Record: exact invocation, expected output, destructive side-effects.

  scaffold-folder:
    step_id: "scaffold-folder"
    type: "task"
    title: "Scaffold skill folder structure"
    task_ref: "task:scaffold-folder"
    meta:
      tool: "skill-creator init_skill.py"
      commands: |
        SKILL="<tool-name>"
        python3 ~/.agents/skills/skill-creator/scripts/init_skill.py "${SKILL}"
        # fallback:
        # mkdir -p "skill/${SKILL}"/{references,scripts,templates}
        # printf -- "---\nname: ${SKILL}\ndescription: |\n  [DRAFT]\n---\n" \
        #   > "skill/${SKILL}/SKILL.md"

  # ── Phase 4: Write core docs (parallel, routellm-routed) ─────────────────────

  write-parallel:
    step_id: "write-parallel"
    type: "parallel"
    title: "Write SKILL.md body, references, and scripts"
    depends_on: ["research-parallel"]
    children: ["write-skill-md", "write-references", "write-scripts"]
    max_concurrency: 3

  write-skill-md:
    step_id: "write-skill-md"
    type: "task"
    title: "Write SKILL.md body"
    task_ref: "task:write-skill-md"
    meta:
      routellm_hint: "strong"
      routellm_model: "router-mf-0.15"
      guidance: |
        Route to strong model — SKILL.md body errors cause agent failures
        in every session that loads this skill.

        Sections: When to Use / Auto-activation, Quick Start (≤ 10 lines,
        runnable), Core Commands/Endpoints table, Key Flags/Options table,
        Common Workflows (3-8 numbered), Agent Loop Contract (if stateful),
        State Machine (ASCII, if applicable), Common Mistakes table,
        Error Handling table (exact error strings as row keys).

        Max 5k words; max 100 chars/line; tables over prose; LLM-reader style.
        Leave frontmatter description as [DRAFT].

  write-references:
    step_id: "write-references"
    type: "task"
    title: "Write references/ deep-dive docs"
    task_ref: "task:write-references"
    meta:
      routellm_hint: "medium"
      routellm_model: "router-mf-0.50"
      guidance: |
        Route at 0.50 — structured reference content; weak model handles well.

        Create references/<topic>.md for each overflow topic:
        api-endpoints.md, config-reference.md, troubleshooting.md,
        data-model.md. Each starts with a one-line summary + link to SKILL.md.
        Add ## References section to SKILL.md linking each file.
        Skip if SKILL.md is under 5k words.

  write-scripts:
    step_id: "write-scripts"
    type: "task"
    title: "Write scripts/ for repetitive operations"
    task_ref: "task:write-scripts"
    meta:
      routellm_hint: "medium"
      routellm_model: "router-mf-0.40"
      guidance: |
        Route at 0.40 — scripts need correctness but are mostly deterministic
        sequences; weak model handles with explicit constraints.

        For each research-ops candidate: write scripts/<op-name>.sh.
        Header: #!/usr/bin/env bash, set -euo pipefail, usage on --help.
        chmod +x each script.
        Add ## Scripts table to SKILL.md (Script | Purpose | Usage).
        Skip if no repetitive ops identified.

  # ── Phase 5: Write description (routellm-routed) ─────────────────────────────

  write-description:
    step_id: "write-description"
    type: "task"
    title: "Write SKILL.md frontmatter description"
    task_ref: "task:write-description"
    depends_on: ["write-parallel"]
    meta:
      routellm_hint: "strong"
      routellm_model: "router-mf-0.10"
      guidance: |
        Route to strong model — description is the only field used for skill
        discovery; a poor description means the skill is never loaded.

        250-350 chars. Template:
          "<Active verb> with <tool-name>. Covers <3-4 key topics>.
           Use when: <trigger 1>, <trigger 2>, <trigger 3>, or
           troubleshooting \"<exact error 1>\" or \"<exact error 2>\"."

        Checklist: active voice, tool named explicitly, 3-5 "Use when:" triggers,
        2-3 copy-pasted error strings, no meta-commentary, 250-350 chars total.
        Write into SKILL.md frontmatter, replacing [DRAFT].

  # ── Phase 6: eva gate — structure and frontmatter ────────────────────────────

  gate-structure:
    step_id: "gate-structure"
    type: "task"
    title: "eva gate: SKILL.md structure and frontmatter valid"
    task_ref: "task:gate-structure"
    depends_on: ["write-description"]
    meta:
      tool: "eva + skill-creator quick_validate"
      commands: |
        SKILL="<tool-name>"
        # Fast local check first
        python3 ~/.agents/skills/skill-creator/scripts/quick_validate.py \
          "skill/${SKILL}"
        # Authoritative eva gate
        eva run \
          --dataset evals/agent-skill/structure-dataset.yaml \
          --no-tui
      eva_contract: "evals/agent-skill/skill-structure.yaml"
      eva_contract_body: |
        name: skill_structure_gate
        provider: filesystem
        evaluators:
          - name: regex
            pattern: "^name: [a-z][a-z0-9-]{0,63}$"
            mode: binary              # name is lowercase-hyphen-case ≤ 64 chars
          - name: regex
            pattern: "Use when:"
            mode: binary              # description has "Use when:" section
          - name: contains
            substring: "## "
            mode: binary              # at least one H2 section
          - name: contains
            substring: "Quick Start"
            mode: binary
          - name: contains
            substring: "Error Handling"
            mode: binary
          - name: regex
            pattern: ".{101,}"
            mode: warn                # lines over 100 chars — warn, not fail

  gate-frontmatter:
    step_id: "gate-frontmatter"
    type: "task"
    title: "eva gate: description length and keyword quality"
    task_ref: "task:gate-frontmatter"
    depends_on: ["gate-structure"]
    meta:
      tool: "eva"
      commands: |
        eva run \
          --dataset evals/agent-skill/frontmatter-dataset.yaml \
          --no-tui
      eva_contract: "evals/agent-skill/skill-frontmatter.yaml"
      eva_contract_body: |
        name: skill_frontmatter_gate
        provider: filesystem
        evaluators:
          - name: regex
            # description must be 250-350 chars (approximate line-count check)
            pattern: "description: \\|"
            mode: binary
          - name: contains
            substring: "Use when:"
            mode: binary
          - name: regex
            # must not contain placeholder text
            pattern: "\\[DRAFT\\]|<.*>"
            mode: binary              # presence = FAIL
          - name: regex
            pattern: "troubleshooting"
            mode: warn                # warn if no troubleshooting keyword

  # ── Phase 7: Validate examples and scripts ───────────────────────────────────

  validate-parallel:
    step_id: "validate-parallel"
    type: "parallel"
    title: "Validate skill quality and examples"
    depends_on: ["gate-frontmatter"]
    children: ["validate-examples", "validate-scripts"]
    max_concurrency: 2

  validate-examples:
    step_id: "validate-examples"
    type: "task"
    title: "Probe every documented flag against live CLI"
    task_ref: "task:validate-examples"
    meta:
      tool: "shell flag probes"
      commands: |
        SKILL="<tool-name>"
        TOOL="<tool-name>"
        # Extract all documented commands and probe --help for each flag
        grep -oP '\`'"${TOOL}"'[^`]+\`' skill/${SKILL}/SKILL.md \
          | sed 's/`//g' | sort -u > /tmp/skill-cmds.txt
        cat /tmp/skill-cmds.txt   # review output
        # Probe each documented subcommand exists
        while IFS= read -r cmd; do
          eval "$cmd --help" > /dev/null 2>&1 \
            && echo "OK: $cmd" || echo "MISSING: $cmd"
        done < /tmp/skill-cmds.txt
      guidance: |
        MISSING lines → update SKILL.md to remove or correct those examples.

  validate-scripts:
    step_id: "validate-scripts"
    type: "task"
    title: "Lint and smoke-test scripts/"
    task_ref: "task:validate-scripts"
    meta:
      tool: "shellcheck"
      commands: |
        SKILL="<tool-name>"
        shellcheck skill/${SKILL}/scripts/*.sh 2>/dev/null || true
        for s in skill/${SKILL}/scripts/*.sh; do
          [ -f "$s" ] || continue
          echo "--- $s ---"
          bash "$s" --help 2>&1 | head -5
        done

  gate-scripts:
    step_id: "gate-scripts"
    type: "task"
    title: "eva gate: scripts are executable and print usage"
    task_ref: "task:gate-scripts"
    depends_on: ["validate-parallel"]
    meta:
      tool: "eva"
      commands: |
        eva run \
          --dataset evals/agent-skill/scripts-dataset.yaml \
          --no-tui
      eva_contract: "evals/agent-skill/skill-scripts.yaml"
      eva_contract_body: |
        name: skill_scripts_gate
        provider: filesystem
        evaluators:
          - name: contains
            substring: "#!/usr/bin/env bash"
            mode: binary
          - name: contains
            substring: "set -euo pipefail"
            mode: binary
          - name: regex
            pattern: "Usage:|usage:"
            mode: binary              # script prints usage
          - name: regex
            pattern: "shellcheck"
            mode: warn                # warn if no shellcheck directive

  # ── Phase 8: Register and smoke-test ─────────────────────────────────────────

  register-skill:
    step_id: "register-skill"
    type: "task"
    title: "Register skill and confirm discovery"
    task_ref: "task:register-skill"
    depends_on: ["gate-scripts"]
    meta:
      tool: "ln + ls"
      commands: |
        SKILL="<tool-name>"
        SKILL_PATH="$(pwd)/skill/${SKILL}"
        ln -sf "${SKILL_PATH}" ~/.agents/skills/${SKILL}
        ln -sf "${SKILL_PATH}" ~/.claude/skills/${SKILL}
        ls -la ~/.claude/skills/${SKILL}/SKILL.md
        ls -la ~/.agents/skills/${SKILL}/SKILL.md
      guidance: |
        Smoke-test: start new session, type "help me use <tool-name>",
        confirm Claude loads the skill.
        If not: check description keywords; verify symlink resolves.
