flow_id: "flow:single-step:1.0"
name: "Single Step"
version: "1.0"
description: "Minimal flow with one step — validates engine bootstrap and run lifecycle"
entry_step: "do-work"
agent:
  name: claude

steps:
  do-work:
    step_id: "do-work"
    type: "task"
    title: "Execute the single task"
    task_template:
      title: "Do work"
      description: |
        Run a single unit of work and report completion.
      requirements:
        capabilities: ["task-execution"]
