flow_id: "flow:pr-review-loop:1.0"
name: "PR Review Loop"
version: "1.0"
description: |
  Automated reviewer→fixer loop for a GitHub PR. Mirrors octorus AI Rally:
  reviewer agent posts feedback, fixer agent applies changes, loop continues
  until approved or max iterations reached.
entry_step: "load-pr-context"

config:
  category: "quality-assurance"
  triggers:
    - "PR ready for review"
    - "feedback applied, re-review needed"
    - "CI passed, needs code review"

  inputs:
    repo:
      description: "GitHub repo slug, e.g. owner/repo"
      required: true
    pr:
      description: "PR number"
      required: true
    working_dir:
      description: "Local clone path (default: cwd)"
      required: false
    max_iterations:
      description: "Max review-fix cycles before giving up (default: 5)"
      required: false

  procedure: |
    ## PR Review Loop

    Automates the review → fix → re-review cycle until a PR is approved or
    max iterations are reached.

    ### States

    ```
    load-pr-context
        ↓
    reviewer-pass          ← iterates back here after each fix
        ↓ request_changes
    fixer-pass
        ↓ completed
    [back to reviewer-pass]
        ↓ approve
    done ✓
    ```

    ### Tool preference

    Use `gh` and `git` for all deterministic operations. Call an LLM step
    only when judgement is required (review quality, fix strategy).

    ### Headless policy (no human in loop)

    | Situation                  | Behavior                              |
    |----------------------------|---------------------------------------|
    | Clarification needed       | Agent proceeds with best judgement    |
    | Dangerous git op requested | Auto-deny; fixer continues without it |
    | Post review/fix to PR      | Auto-post when `auto_post = true`     |

steps:

  # ── Phase 1: context ────────────────────────────────────────────────────────

  load-pr-context:
    step_id: "load-pr-context"
    type: "task"
    title: "Fetch PR metadata and diff"
    task_template:
      title: "Load PR context for review loop"
      description: |
        Collect all context needed by reviewer and fixer.

        ## Tool steps (no LLM needed)

        ```bash
        # PR metadata
        gh pr view {{repo}} --pr {{pr}} \
          --json number,title,body,headRefName,baseRefName,headRefOid \
          > /tmp/pr-meta.json

        # Full diff (for reviewer prompt)
        gh pr diff {{repo}} --pr {{pr}} > /tmp/pr-diff.patch

        # CI status
        gh pr checks {{repo}} --pr {{pr}} --json name,status,conclusion \
          > /tmp/pr-checks.json

        # External bot comments (Copilot, CodeRabbit, etc.)
        gh api repos/{{repo}}/pulls/{{pr}}/comments \
          --paginate \
          --jq '[.[] | select(.user.login | endswith("[bot]"))]' \
          > /tmp/pr-bot-comments.json
        ```

        Save outputs to /tmp/pr-{meta,diff,checks,bot-comments}.json.
        These are the sole inputs to subsequent steps.
      requirements:
        tools: ["gh", "git"]

  # ── Phase 2: reviewer pass ───────────────────────────────────────────────────

  reviewer-pass:
    step_id: "reviewer-pass"
    type: "task"
    title: "Reviewer agent: analyze diff and produce structured feedback"
    depends_on: ["load-pr-context"]
    task_template:
      title: "Run reviewer agent on PR diff"
      description: |
        Read /tmp/pr-{meta,diff,bot-comments}.json.
        Produce structured review feedback.

        ## Reviewer role

        - Read-only: no git writes, no file edits
        - Allowed gh commands: `gh pr view`, `gh pr diff`, `gh pr checks`,
          `gh api --method GET`
        - Focus: correctness, security, perf, style, missing tests/docs

        ## Output (JSON to stdout, save to /tmp/pr-review-N.json)

        ```json
        {
          "action": "approve|request_changes|comment",
          "summary": "<one paragraph>",
          "comments": [
            {
              "path": "src/foo.go",
              "line": 42,
              "body": "<specific feedback>",
              "severity": "critical|major|minor|suggestion"
            }
          ],
          "blocking_issues": ["<issue 1>", "<issue 2>"]
        }
        ```

        If `action == "approve"`: flow exits success.
        If `action == "request_changes"`: fixer-pass runs next.
        If `action == "comment"`: post comments; check blocking_issues to decide
        whether fixer-pass is needed.

        ## Post review to PR

        ```bash
        # Post inline comments via gh
        gh api repos/{{repo}}/pulls/{{pr}}/reviews \
          --method POST \
          --field body="$(jq -r .summary /tmp/pr-review-N.json)" \
          --field event="$(jq -r .action /tmp/pr-review-N.json | tr 'a-z' 'A-Z')" \
          --field "comments=$(jq .comments /tmp/pr-review-N.json)"
        ```
      requirements:
        capabilities: ["code-review", "security-review"]
        tools: ["gh", "git"]

  check-approval:
    step_id: "check-approval"
    type: "branch"
    title: "Check if PR is approved; route to done or fixer"
    depends_on: ["reviewer-pass"]
    cases:
      - when: "action == 'approve'"
        next: "done"
      - when: "action == 'request_changes'"
        next: "fixer-pass"
    # default_next handles 'comment' action (blocking_issues present → fixer; none → done-equivalent)
    default_next: "fixer-pass"
    meta:
      guidance: |
        Read `action` from /tmp/pr-review-N.json.
        Valid values: approve | request_changes | comment.
        'comment' with blocking_issues → treat as request_changes (default_next).

        Also check iteration count against max_iterations (default 5).
        If count >= max: surface warning and halt without merge.

  # ── Phase 3: fixer pass ──────────────────────────────────────────────────────

  fixer-pass:
    step_id: "fixer-pass"
    type: "task"
    title: "Fixer agent: apply changes from review feedback"
    task_template:
      title: "Run fixer agent on review comments"
      description: |
        Read /tmp/pr-review-N.json.
        Apply all blocking issues and major comments.

        ## Fixer role

        - File ops: Read, Edit, Write, Glob, Grep
        - Git (safe subset only): status, diff, add, commit, log, show,
          branch, switch, stash
        - Forbidden git ops: reset --hard, clean -fd, push --force, push
          (unless user explicitly grants)
        - gh: pr view, pr diff, pr checks, api GET only

        ## Critical rules

        - NEVER `git reset --hard` — destroys work
        - NEVER `git clean -fd` — deletes untracked files
        - Commit changes locally after each fix batch
        - If unclear: output `status: needs_clarification` + question

        ## Output (JSON to stdout, save to /tmp/pr-fix-N.json)

        ```json
        {
          "status": "completed|needs_clarification|needs_permission|error",
          "summary": "<what was done>",
          "files_modified": ["src/foo.go", "src/bar.go"],
          "question": "<if needs_clarification>",
          "permission_request": {
            "action": "git push origin HEAD",
            "reason": "reviewer requires changes pushed before re-review"
          }
        }
        ```

        ## Commit pattern

        ```bash
        git add <files>
        git commit -m "fix: address review feedback (iteration N)"
        ```
      requirements:
        capabilities: ["code-modification", "refactoring"]
        tools: ["git", "gh"]

  handle-fixer-output:
    step_id: "handle-fixer-output"
    type: "task"
    title: "Handle fixer output: clarification, permission, or re-review"
    depends_on: ["fixer-pass"]
    task_template:
      title: "Route fixer result to next action"
      description: |
        ```bash
        STATUS=$(jq -r .status /tmp/pr-fix-N.json)
        ```

        | status                | action                                       |
        |-----------------------|----------------------------------------------|
        | `completed`           | post fix summary to PR, loop → reviewer-pass |
        | `needs_clarification` | surface question; answer → fixer continues   |
        | `needs_permission`    | surface request; deny if dangerous git op    |
        | `error`               | halt, surface error                          |

        ### Post fix summary to PR

        ```bash
        SUMMARY=$(jq -r .summary /tmp/pr-fix-N.json)
        gh pr comment {{pr}} --repo {{repo}} --body "$SUMMARY"
        ```

        ### Dangerous git op check (auto-deny)

        ```bash
        ACTION=$(jq -r '.permission_request.action // ""' /tmp/pr-fix-N.json)
        SAFE_CMDS="status|diff|add|commit|log|show|branch|switch|stash"
        if echo "$ACTION" | grep -qvE "^git ($SAFE_CMDS)"; then
          echo "Auto-denied: $ACTION"
          # fixer continues without the permission
        fi
        ```

        When status == completed: increment iteration counter and loop back
        to reviewer-pass (up to max_iterations).
      requirements:
        tools: ["jq", "gh", "bash"]

  # ── Phase 4: done ────────────────────────────────────────────────────────────
  # Note: the loop-back from handle-fixer-output → reviewer-pass cannot be
  # expressed as a DAG edge. The loop is driven by the agent following the
  # procedure instructions in handle-fixer-output until max_iterations is hit.

  done:
    step_id: "done"
    type: "task"
    title: "PR approved — emit final summary"
    task_template:
      title: "Emit final rally result"
      description: |
        ```bash
        ITER=$(cat /tmp/pr-iteration-count.txt)
        SUMMARY=$(jq -r .summary /tmp/pr-review-N.json)

        cat <<EOF
        {
          "result": "Approved",
          "iterations": $ITER,
          "summary": "$SUMMARY"
        }
        EOF
        ```

        Exit code 0 = approved.
        Exit code 1 = not approved / max iterations / error.
      requirements:
        tools: ["jq", "bash"]
