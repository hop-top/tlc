# Audit: T-0001 to T-0010 Metadata Sync

Date: 2026-03-28
Author: $USER
Task: tlc/T-0102

## Summary

Early tasks T-0001–T-0010 had stale or incorrect metadata. A prior audit run
(same date, 17:45) updated T-0001–T-0004 correctly but assigned "aom:" prefixed
titles to T-0005–T-0010, which was wrong for this project's task DB.

## Method

Cross-referenced `tlc task show` output against `git log` merge commits for
each task ID. Also compared against the `aom` project's own task DB
(`tlc -c /path/to/aom/.tlc/config.yaml task list`).

## Findings per Task

| ID      | Verdict    | Action                                               |
|---------|------------|------------------------------------------------------|
| T-0001  | Correct    | Confirmed by merge e2c911d (projects table migration)|
| T-0002  | Correct    | Confirmed by merge f9d968f (init reconnect)          |
| T-0003  | Correct    | Confirmed by merge 7690b43 (workspace config schema) |
| T-0004  | Correct    | Confirmed by merge 70998ab (space adapter)           |
| T-0005  | Unresolved | No merge commit in tlc repo; ID likely created here  |
|         |            | but work tracked in aom project (aom/T-0005).        |
| T-0006  | Unresolved | Same pattern; aom/T-0006 = "Copy pkg/reconcile".     |
| T-0007  | Unresolved | Same pattern; aom/T-0007 = "Copy pkg/analytics".     |
| T-0008  | WRONG      | Git: merge 5cdf8c2 = ProfileResolver (core/profile)  |
|         |            | Prior audit set: "aom: scaffold cmd/aom CLI" — wrong.|
|         |            | Fixed to: "core: add ProfileResolver mapping..."     |
| T-0009  | Unresolved | No merge commit; aom/T-0009 = "Copy test fixtures".  |
| T-0010  | WRONG      | Git: merge a153e59 = XDG data migration (config)     |
|         |            | Prior audit set: "aom: scaffold documentation" — wrong|
|         |            | Fixed to: "config: migrate global data path to XDG"  |

## Root Cause

Tasks T-0005–T-0011 (aom-tagged) were created in tlc's task DB on 2026-03-08,
likely by an agent that was scaffolding the `aom` project but used the tlc
project's task tracker. The `aom` project has its own `.tlc/` DB with the same
IDs (T-0001–T-0011) mapping to aom scaffolding steps. The correct aom work
belongs in `/Users/jadb/.w/ideacrafterslabs/aom/.tlc/`, not here.

## Actions Taken

- T-0001–T-0004: verified correct, no change.
- T-0005, T-0006, T-0007, T-0009: title set to "UNRESOLVED: task ID not
  traceable in tlc git history"; tag set to `audit-unresolved`; description
  documents the ambiguity.
- T-0008: title and description corrected to reflect ProfileResolver work.
  Tag `aom` removed; tags `core`, `profile` added.
- T-0010: title and description corrected to reflect XDG UserDataDir migration.
  Tag `aom` removed; tags `config`, `xdg` added.
